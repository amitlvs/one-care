import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hospital.app',
  appName: 'hospital',
  webDir: 'dist/hospital',
  bundledWebRuntime: false,
};

export default config;
